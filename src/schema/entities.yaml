---
# Ordering and attributes of entities
- subject:
    entity: sub
    format: label
- session:
    entity: ses
    format: label
- task:
    entity: task
    format: label
- acquisition:
    entity: acq
    format: label
- ceagent:
    entity: ce
    format: label
- reconstruction:
    entity: rec
    format: label
- direction:
    entity: dir
    format: label
    metadata:
      - PhaseEncodingDirection:
          requirement: required
- run:
    entity: run
    format: index
- modality:
    entity: mod
    format: label
- echo:
    entity: echo
    format: index
    metadata:
      - EchoTime:
          requirement: required
- recording:
    entity: recording
    format: label
- processing:
    entity: proc
    format: label
- space:
    entity: space
    format: label
- split:
    entity: split
    format: index
